[project]
name = "support-agent"
version = "0.1.0"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["core", "retrieval", "ingestion", "agent", "api", "evaluation"]